void loop()
{
    loop();
}

int fib(int i)
{
    if (i==0 || i==1) return 1;
    return fib(i-1)+fib(i-2);
}

int main()
{
    while(true);
    //loop();
    char buf[1000000000];
    buf[0]=0;
    return 0;
}
