# this doesn't need to be run through rshell!
# you can test your ls using bash
#
# e.g.
# cs100-runtests bash

# test regular ls
bin/ls
# test ls -a
bin/ls -a
# test ls -l
bin/ls -l
# test ls -R
bin/ls -R
